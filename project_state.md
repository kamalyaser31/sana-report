# حالة المشروع (Project State)

- **اسم المشروع:** نظام تقارير أكاديمية سنا (`sana-report`)
- **الرابط المباشر:** `https://kamalyaser31.github.io/sana-report/`
- **المستودع:** `https://github.com/kamalyaser31/sana-report`

## الهيكلية المعتمدة (Modular Flat Structure):
- `index.html`: المدخل الأساسي متوافق مع GitHub Pages و PWA وشريط إدارة المسودات الرشيق (`#draftsBar`).
- `tajweed.html`: صفحة مستقلة وخفيفة مخصصة لحلقات التجويد الجماعية، بحقولها الضرورية ومربع تقرير الحصة والإرسال المباشر للاستمارة دون بطاقات الطلاب.
- `manifest.json` & `sw.js`: دعم PWA والتخزين المؤقت للإصدار الرابع عشر (`sana-report-v14`) مع استراتيجية Network-First وتجاوز كاش HTTP التلقائي.
- `css/styles.css`: التصميم الزمردي المتقن، والوضع الليلي، وإشعارات التوست، وشريط المسودات الرشيق بعد تطهير المحددات الخاملة.
- `js/`:
  - `constants.js`: الثوابت والنصوص الرسمية ومصفوفة المعلمين `TEACHERS` ومعرفات استمارة Google Forms وترتيب حقول الطلاب المعتمد (التسميع، الماضي، الماضي القريب، الحفظ الجديد، الماضي المطلوب، الماضي القريب المطلوب).
  - `state.js`: إدارة الحالة العامة ومخزن البيانات المحلي ومصدر الحقيقة المشترك (SoT).
  - `ui.js`: دوال الهروب والتأمين وإشعارات التوست السريعة.
  - `drafts.js`: إدارة مسودات الحلقات المتعددة (`sana_drafts`) والتبديل السريع بينها.
  - `settings.js`: تفضيلات المظهر والجلسة وتخصيص إظهار الحقول.
  - `render.js`: توليد وبناء بطاقات الطلاب وشريط الإحصائيات في شجرة DOM.
  - `students.js`: عمليات الطلاب (إضافة، حذف، تحديث فوري، تدوير الواجبات).
  - `report.js`: صياغة نصوص التقارير الرسمية، النسخ، التصدير لـ PDF، والإرسال لـ Google Forms.
  - `quran.js`: واجهة منتقي المصحف الشريف والتفاعل مع السور والصفحات والأجزاء.
  - `app.js`: نقطة انطلاق التطبيق المقتضبة وربط أحداث البداية واختصار Escape.
  - `quran-data.js`: محرك منتقي المصحف وحسابات الصفحات (1-604) والسور والأجزاء.
  - `html2pdf.bundle.min.js`: حزمة التصدير لـ PDF محلياً.
- `data/quran_surahs.json` & `data/quran_juzs.json`: بيانات سور وأجزاء القرآن المحملة من GitHub.
- `assets/icon.svg`: أيقونة التطبيق الرسمية.
- `CONTEXT.md`: مسرد المصطلحات المعمارية لربط السحاب ومرآة التخزين المحلي.
- `docs/adr/`: سجلات القرارات المعمارية المستقرة:
  - `0001-supabase-single-table-state.md`: اعتماد مرآة التخزين المحلي السحابية بجدول مفرد `teacher_profiles`.
  - `0002-submitted-reports-archive.md`: أرشفة نصوص التقارير المعتمدة في جدول `reports` عند الإرسال.
  - `0003-double-submit-prevention.md`: نظام الحصانة ثلاثي الطبقات لمنع تكرار الإرسال والنقر المزدوج.
  - `0004-one-click-parent-whatsapp.md`: مراسلة أولياء الأمور بنقرة واحدة عبر روابط واتساب العميقة (`wa.me`).
  - `0005-digital-award-badge.md`: توليد بطاقة الوسام الرقمية محلياً عبر Canvas دون مكتبات.
  - `0006-unmemorized-today-toggle.md`: وسم "لم يحفظ اليوم" وتجميد ترحيل الحفظ الجديد تلقائياً.
  - `0007-simple-session-stopwatch.md`: ساعة توقف تصاعدية بسيطة في شريط الرأس دون تنبيهات صوتية.
  - `0008-predefined-notes-picker.md`: بنك الملاحظات التربوية الجاهزة مع إتاحة التحرير اليدوي الحر.
- `docs/ROADMAP_FUTURE_AUTOMATION.md`: خارطة طريق الترقية المستقبلية (Phase 2) للربط المباشر مع محرك الأتمتة (`doPost`) والاستغناء عن Google Forms.
- `README.md` & `CHANGELOG.md`: التوثيق وسجل التحديثات التراكمي وبيانات المطور.

## حالة التطوير الراهنة (Current Milestone)
- تم حل خطأ 400 Bad Request نهائياً بإلغاء تمرير نص التقرير في رابط الاستمارة، وتعبئة الحقول الإدارية فقط (المعلم، الفئة، الوقت) مع نسخ نص التقرير تلقائياً في الحافظة ليلصقه المعلم يدوياً، وترقية كاش PWA إلى sana-report-v14.
- تم حل مشكلة تأخر التحديثات بتطبيق استراتيجية Network-First وتجاوز كاش HTTP وإعادة التحميل التلقائي.
- تم ربط فئة الطلاب وفترة الحلقة بكل مسودة على حدة وبناء صفحة التجويد المستقلة tajweed.html.
